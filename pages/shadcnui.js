import { Button } from '@shadcn/ui';

export default function Home() {
  return (
    <div className="flex justify-center items-center h-screen bg-gray-100">
      <Button variant="primary">Hello from ShadCN UI</Button>
    </div>
  );
}
